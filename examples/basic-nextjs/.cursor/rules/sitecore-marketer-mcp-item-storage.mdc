---
description: Sitecore Marketer MCP item storage locations - guides AI agents on where items should be created and stored when using the Sitecore Marketer MCP tools and Agent API
globs: ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", "**/*.json", "**/*.yml", "**/*.yaml"]
alwaysApply: false
---

# Sitecore Marketer MCP — Item Storage Locations

When working with the Sitecore Marketer MCP (Model Context Protocol) to create, manage, or reference items in Sitecore, always follow the documented content tree structure below. The Marketer MCP connects AI agents to Sitecore via the Agent API and each tool maps to an endpoint that performs actions in the platform. Items must be stored in their correct locations to ensure proper functionality, publishing, serialization, and workflow compliance.

## Quick Start: XM Cloud Component Development

The most common workflow when building headless applications with XM Cloud:

1. **Create Templates** → Define data structure for your component
2. **Create Rendering** → Link Sitecore item to React component
3. **Create Datasource Folder** → Organize content items
4. **Create Datasource Items** → Add actual content
5. **Register Component** → Add to component-map.ts
6. **Add to Page** → Use Pages editor to add component
   - **List components:** If the component has parent/child datasources, also add `_HorizonDatasourceGrouping` as a base template to the parent template so children appear in the Pages Content tab (see [Children in XM Cloud Pages Content Tab](#children-in-xm-cloud-pages-content-tab)).

See detailed sections below for each step.

---

## Marketer MCP Tools Overview

The Marketer MCP exposes tools grouped by use case. Each tool corresponds to an Agent API endpoint. When creating items through these tools, the items land in specific content tree locations depending on their type.

---

## XM Cloud Headless Components

### Component Templates (created via `create_content_item`)

Component templates define the data structure for headless components.

**Location:** `/sitecore/templates/Project/<site-collection>/`

**Template Folder Structure:**

Templates must NOT be created loose under the site-collection root. They must be organized into category subfolders that group related templates. Use Template Folder items (Template ID: `0437fee2-44c9-46a6-abe9-28858d9fee8c`) to create the folder hierarchy.

```
/sitecore/templates/Project/<site-collection>/
  ├── Components/                          ← Component templates (most common)
  │   ├── Banners/
  │   │   ├── Hero (Template)
  │   │   └── HeroBanner (Template)
  │   ├── Cards/
  │   │   ├── ArticleCards (Template)
  │   │   └── ArticleCard (Template)
  │   ├── Navigation/
  │   │   ├── Header (Template)
  │   │   └── Footer (Template)
  │   └── Forms/
  │       └── ContactForm (Template)
  ├── Folders/                             ← Folder templates for datasource organization
  │   ├── Hero Folder (Template)
  │   ├── Article Cards Folder (Template)
  │   └── Navigation Folder (Template)
  ├── Presentation/                        ← Page-level and layout templates
  ├── Site/                                ← Site configuration templates
  ├── Sites/                               ← Multi-site templates
  └── Taxonomy/                            ← Classification and tagging templates
```

**Key rules for template folder organization:**

- Always create a category folder before creating templates — never put templates directly under the site-collection root
- `Components/` is the main folder for all component data templates, further subdivided by component category (Banners, Cards, Navigation, Forms, etc.)
- `Folders/` stores the folder templates used for datasource organization (e.g., "Hero Folder", "Article Cards Folder")
- Group related parent and child templates in the same subfolder (e.g., `ArticleCards` and `ArticleCard` both go under `Components/Cards/`)
- Folder names use PascalCase or descriptive names matching the component group
- When in doubt about which subfolder to use, check the existing structure first with `get_content_item_by_path` before creating new folders

**Creation Workflow — Folders first, then templates:**

```
1. Check if category folder exists:
   get_content_item_by_path(path="/sitecore/templates/Project/mysite/Components/Cards")

2. If not, create the category folder:
   create_content_item(name="Cards", templateId="0437fee2-44c9-46a6-abe9-28858d9fee8c", parentId="<components-folder-id>")

3. Create template inside the folder:
   create_content_item(name="ArticleCard", templateId="ab86861a-6030-46c5-b394-e8f99e8b87db", parentId="<cards-folder-id>")

4. Create section and fields as children of the template (see below)
```

**Template Item Structure (3-level hierarchy):**

1. **Template Item** (Template ID: `ab86861a-6030-46c5-b394-e8f99e8b87db`)
   - The parent template definition
   - Name: Your component name in PascalCase (e.g., "ArticleCard", "Hero")

2. **Template Section** (Template ID: `e269fbb5-3750-427a-9149-7aa950b49301`)
   - Child of template item
   - Name: Usually "Data"
   - Groups related fields together

3. **Template Field Items** (Template ID: `455a3e98-a627-4b40-8035-e683a0331ac7`)
   - Children of template section
   - Each field is a separate item
   - Set the `Type` field to specify field type

**Common Field Types:**

- `Single-Line Text` — Short text values (titles, names)
- `Multi-Line Text` — Longer text content
- `Rich Text` — HTML formatted content
- `Image` — Media library images
- `General Link` — Internal/external links
- `Checkbox` — Boolean values
- `Date` / `DateTime` — Date/time values
- `Number` / `Integer` — Numeric values
- `Droplink` — Single item reference
- `Multilist` — Multiple item references

**Setting field values via MCP (`create_content_item` / `update_content`):**

Some field types require specific formats when passed in the `fields` payload:

- **Single-Line Text / Multi-Line Text:** Plain string value.
  `"Title": "My Article Title"`

- **Rich Text:** HTML string.
  `"Description": "<p>Some <strong>rich</strong> content</p>"`

- **General Link:** Link XML format. The value must be a Sitecore link XML string:
  `"CtaLink": "<link linktype=\"external\" url=\"https://example.com\" text=\"Learn more\" target=\"_blank\" />"`
  `"CtaLink": "<link linktype=\"internal\" id=\"{ITEM-GUID}\" text=\"Read more\" />"`
  `"CtaLink": "<link linktype=\"external\" url=\"#\" text=\"Learn more about Saga\" />"`

- **Image:** Media library item path or ID.
  `"Image": "<image mediaid=\"{MEDIA-ITEM-GUID}\" />"`

- **Checkbox:** `"1"` for true, `""` for false.
  `"IsActive": "1"`

- **Date / DateTime:** ISO format string.
  `"PublishDate": "20250217T120000Z"`

**Example Template Structure:**

```
/sitecore/templates/Project/mysite/Components/Cards/
  └── ArticleCard (Template)
      └── Data (Template Section)
          ├── Title (Template Field, Type: "Single-Line Text")
          ├── Image (Template Field, Type: "Image")
          ├── Link (Template Field, Type: "General Link")
          └── BadgeText (Template Field, Type: "Single-Line Text")
```

**Creation Workflow — Template items:**

```
1. Create template (inside the correct category folder):
   create_content_item(name="ArticleCard", templateId="ab86861a-6030-46c5-b394-e8f99e8b87db", parentId="<cards-folder-id>")

2. Create section:
   create_content_item(name="Data", templateId="e269fbb5-3750-427a-9149-7aa950b49301", parentId="<template-id>")

3. Create fields:
   create_content_item(name="Title", templateId="455a3e98-a627-4b40-8035-e683a0331ac7", parentId="<section-id>", fields={"Type": "Single-Line Text"})
```

---

### Component Renderings (created via `create_content_item`)

Renderings link Sitecore items to front-end React components and enable them in the Pages editor.

**Location:** `/sitecore/layout/Renderings/Project/<site-collection>/`
**Template:** JSON Rendering (Template ID: `04646a89-996f-4ee7-878a-ffdbf1f0ef0d`)

**Rendering Folder Structure:**

Like templates, renderings must be organized into category subfolders that mirror the template folder structure. Use Rendering Folder items to create the hierarchy.

```
/sitecore/layout/Renderings/Project/<site-collection>/
  ├── Banners/
  │   ├── Hero (JSON Rendering)
  │   └── HeroBanner (JSON Rendering)
  ├── Cards/
  │   └── ArticleCards (JSON Rendering)
  ├── Navigation/
  │   ├── Header (JSON Rendering)
  │   └── Footer (JSON Rendering)
  └── Forms/
      └── ContactForm (JSON Rendering)
```

**Key rules for rendering folder organization:**

- Mirror the template `Components/` subfolder structure — if the template is under `Components/Cards/`, put the rendering under `Cards/`
- One rendering per component — the rendering name matches the component (e.g., "ArticleCards" rendering for the ArticleCards component)
- Check existing folder structure first with `get_content_item_by_path` before creating new folders

#### CRITICAL FIELDS (all required):

**1. `componentName` — MUST use kebab-case matching the component filename**

The `componentName` field must match the component-map key exactly, which uses kebab-case derived from the filename.

**Naming Convention Chain (all must match):**

```
Component file:        article-cards.tsx       (kebab-case)
Component-map import:  import * as articlecards from '...'
Component-map key:     ['article-cards', { ...articlecards }]
Rendering field:       componentName: "article-cards"   ← MUST MATCH MAP KEY
```

Examples:

- ✅ `"article-cards"` — matches `article-cards.tsx` and map key
- ✅ `"hero"` — matches `hero.tsx` and map key
- ❌ `"ArticleCards"` — PascalCase does not match
- ❌ `"articlecards"` — missing hyphen

**2. `Datasource Template` — Complete path to the datasource template (NOT a GUID)**

This tells Sitecore what template to use when creating new datasources and what data structure to expect.

Format: `/sitecore/templates/Project/<site-collection>/Path/To/Template`

Examples:

- ✅ `/sitecore/templates/Project/mysite/Components/Cards/ArticleCards`
- ✅ `/sitecore/templates/Project/click-click-launch/Components/Banners/Hero`
- ❌ `/sitecore/templates/Project/mysite/ArticleCards` — missing category subfolder
- ❌ `{e0bd6abd-50c2-429a-baa0-ed13517e7055}` — GUID format not accepted
- ❌ Empty — causes "No datasource template found" error (exception: context-only components that have no datasource; see [Context-Only Components](#context-only-components-no-datasource) below)

**3. `Datasource Location` — Query defining where datasources can be stored and selected**

Uses Sitecore query syntax with `@@name` and `@@templatename`. Multiple locations are separated with pipe `|`.

**Common Query Patterns:**

Site-specific only:

```
query:$site/*[@@name='Data']/*[@@templatename='Hero Folder']
```

Site + shared sites:

```
query:$site/*[@@name='Data']/*[@@templatename='Hero Folder']|query:$sharedSites/*[@@name='Data']/*[@@templatename='Hero Folder']
```

With nested folder structure:

```
query:$site/*[@@name='Data']/*[@@templatename='Hero Sections Folder']|query:$sharedSites/*[@@name='Data']/*[@@templatename='Hero Sections Folder']
```

**Query Syntax Breakdown:**

- `$site` — Current site content root
- `$sharedSites` — Shared content root across sites
- `/*[@@name='Data']` — Match child item named "Data"
- `/*[@@templatename='Hero Folder']` — Match children using the specified folder template
- `|` — OR separator for multiple query locations

**4. `Data source` — Leave EMPTY** (do not set a template ID here)

**5. `Parameters Template` — Optional**, for component-level parameters

**6. `AddFieldEditorButton` — Set to `"1"`** to enable the field editor button in Pages

**7. `Component GraphQL Query` (internal MCP field name: `ComponentQuery`) — REQUIRED for components with parent/children datasource structures**

This field shapes the JSON data passed to the component. Variables `$datasource`, `$contextItem`, and `$language` are always available. If left blank, normal JSS shaping is used — but for any component that has a parent datasource with child items (list-based components), you MUST provide a GraphQL query that fetches both the parent fields and the children fields.

**Important:** The Content Editor UI label shows "Component GraphQL Query", but when updating the rendering via MCP (e.g., `update_content` with `itemId`, `siteName`, `fields`), use the internal field name `ComponentQuery` in the fields payload.

**When to use vs. when to leave empty:**

- **Use a Component GraphQL Query** only when the component has a parent/children (list) datasource and needs `children.results` in props. Examples: ArticleCards, MultiPromo, FAQ lists, card grids with child items.
- **Leave Component GraphQL Query empty** for single-datasource components with no children (e.g., Hero, Promo, simple Banners). The layout service will use default JSS shaping and a different props shape (see "Props Shape: With vs Without Component GraphQL Query" below).

When the component does not need children functionality, do not add a GraphQL query to the rendering; leave the field empty.

**Why this is critical:** Without this query, the component will only receive the parent datasource fields and will NOT receive the children data. The `children { results { ... } }` block in the query is what makes child items available to the React component via `fields.data.datasource.children.results`.

**Query Structure:**

- The query name should match the parent template name
- Use `$datasource` and `$language` as query variables
- Use `... on <ParentTemplateName>` to type the parent and access its fields
- Use `children { results { ... on <ChildTemplateName> { } } }` to fetch child items
- Every field you want available in the React component must be explicitly listed with `{ jsonValue }`

**Example — MultiPromo (parent with SimplePromo children):**

```graphql
query MultiPromo($datasource: String!, $language: String!) {
  datasource: item(path: $datasource, language: $language) {
    ... on MultiPromo {
      title {
        jsonValue
      }
      description {
        jsonValue
      }
    }
    children {
      results {
        ... on SimplePromo {
          id
          heading {
            jsonValue
          }
          image {
            jsonValue
          }
          link {
            jsonValue
          }
          description {
            jsonValue
          }
        }
      }
    }
  }
}
```

**Example — ArticleCards (parent with ArticleCard children):**

```graphql
query ArticleCards($datasource: String!, $language: String!) {
  datasource: item(path: $datasource, language: $language) {
    ... on ArticleCards {
      title {
        jsonValue
      }
    }
    children {
      results {
        ... on ArticleCard {
          id
          title {
            jsonValue
          }
          image {
            jsonValue
          }
          link {
            jsonValue
          }
          badgeText {
            jsonValue
          }
        }
      }
    }
  }
}
```

**Key Rules for the GraphQL Query:**

- Template type names in `... on <TemplateName>` use PascalCase matching the Sitecore template name
- Field names in the query use camelCase (e.g., `badgeText` not `BadgeText`)
- Always include `id` in children results so React can use it as a key
- Always wrap each field with `{ jsonValue }` so the Content SDK can render them as editable
- If a field is missing from the query, it will NOT be available in the component props
- The query alias `datasource:` must be used for the root `item()` call

**Common Mistakes:**

- ❌ Leaving this field blank for list components — children data will be missing. For list components, you must set the query and use `fields.data.datasource` and `datasource.children.results` in the component.
- ❌ Adding a GraphQL query to a simple component (e.g., Hero) that has no children — unnecessary and changes the props shape. Leave it empty and use the default PascalCase/`.value` shape.
- ❌ Forgetting `{ jsonValue }` on fields — fields won't be editable in Pages
- ❌ Using wrong template name in `... on` fragments — data won't resolve
- ❌ Omitting `id` from children — React will lack unique keys for list rendering
- ❌ Writing a component with `fields.data.datasource` and `jsonValue` when the rendering has no query — the component will receive `fields.Title`, `fields.Description` with `.value` instead
- ❌ Looking for children at `fields.data.children.results` (sibling to datasource) — children are nested **inside** datasource: `fields.data.datasource.children.results`

#### Context-Only Components (No Datasource)

Some components read from **page context** instead of a datasource. They use the current page's own route fields or context path rather than a separately selectable datasource item. Examples: breadcrumbs, article title/excerpt, article image/meta, article body content.

**Rendering configuration for context-only components:**

- `componentName`: kebab-case as usual (e.g., `"breadcrumbs"`, `"article-content"`)
- `Datasource Template`: **empty** (no datasource needed)
- `Datasource Location`: **empty**
- `ComponentQuery`: **empty**
- `Data source`: **empty**
- `AddFieldEditorButton`: `"1"` (still useful for parameter editing)

**Props shape:** Context-only components receive no `fields` data from a datasource. Instead, they read from the `page` prop:

- **Route fields:** `page.layout.sitecore.route.fields` — contains the current page item's fields (e.g., Title, Excerpt, Content, MainImage)
- **Context path:** `page.layout.sitecore.context.itemPath` — useful for breadcrumbs to determine hierarchy
- **Route name:** `page.layout.sitecore.route.name` — the current page's item name

**Example interface — context-only component:**

```typescript
interface ArticleTitleExcerptProps extends ComponentProps {
  params: { [key: string]: any };
}

export const Default: React.FC<ArticleTitleExcerptProps> = (props) => {
  const { page } = props;
  const routeFields = page?.layout?.sitecore?.route?.fields;
  const title = routeFields?.Title;
  const excerpt = routeFields?.Excerpt;

  return (
    <header>
      {title && <Text field={title} />}
      {excerpt && <Text field={excerpt} />}
    </header>
  );
};
```

**When to use context-only (no datasource):**

- Breadcrumbs (reads page hierarchy from context path)
- Article header blocks that display the current page's own Title, Excerpt, Author, PublishedDate
- Article body content that renders the current page's Rich Text Content field
- Any component that always displays the current page's own fields and has no need for a datasource picker

**When NOT to use context-only:**

- Components where content authors need to select or switch between different datasource items
- Components that appear on multiple pages with different content (use a datasource instead)
- Components with parent/child data structures (use datasource + ComponentQuery)

**No datasource picker:** When adding a context-only component to a page, no datasource picker is shown — the component is simply added and immediately reads from the page's own fields. Ensure the page template includes the expected fields (e.g., the Article Page template should have Title, Excerpt, Content, MainImage, Author, PublishedDate fields).

#### Complete Rendering Example:

```
Name: ArticleCards
Location: /sitecore/layout/Renderings/Project/mysite/Cards/ArticleCards
Template: JSON Rendering
Fields:
  componentName:          article-cards
  Datasource Template:    /sitecore/templates/Project/mysite/Components/Cards/ArticleCards
  Datasource Location:    query:$site/*[@@name='Data']/*[@@templatename='Article Cards Folder']|query:$sharedSites/*[@@name='Data']/*[@@templatename='Article Cards Folder']
  Data source:            (empty)
  AddFieldEditorButton:   1
  ComponentQuery:         query ArticleCards($datasource: String!, $language: String!) { datasource: item(path: $datasource, language: $language) { ... on ArticleCards { title { jsonValue } } children { results { ... on ArticleCard { id title { jsonValue } image { jsonValue } link { jsonValue } badgeText { jsonValue } } } } } }
```

Note: The field is labeled "Component GraphQL Query" in the Content Editor UI, but use `ComponentQuery` as the field name when setting it via MCP.

#### Creation Workflow:

```
1. Create rendering (inside the correct category folder):
   create_content_item(
     name="ArticleCards",
     templateId="04646a89-996f-4ee7-878a-ffdbf1f0ef0d",
     parentId="<cards-renderings-folder-id>"
   )

2. Update rendering fields (use update_content or update_fields_on_content_item):
   update_content(
     itemId="<rendering-id>",
     siteName="mysite",
     fields={
       "componentName": "article-cards",
       "Datasource Template": "/sitecore/templates/Project/mysite/Components/Cards/ArticleCards",
       "Datasource Location": "query:$site/*[@@name='Data']/*[@@templatename='Article Cards Folder']|query:$sharedSites/*[@@name='Data']/*[@@templatename='Article Cards Folder']",
       "AddFieldEditorButton": "1",
       "ComponentQuery": "query ArticleCards($datasource: String!, $language: String!) { datasource: item(path: $datasource, language: $language) { ... on ArticleCards { title { jsonValue } } children { results { ... on ArticleCard { id title { jsonValue } image { jsonValue } link { jsonValue } badgeText { jsonValue } } } } } }"
     }
   )
```

**Note on updating items via MCP:** Two update tools are available, each suited for different situations:

- **`update_content`** — Requires `itemId`, `siteName` (e.g., `"saga-group"`), and `fields`. Use for content items that live under a site (pages, datasources, content items).
- **`update_fields_on_content_item`** — Requires `itemId` and `fields` only, no `siteName`. Use for templates, renderings, or items outside a site context.

Both achieve the same result for field updates. For the GraphQL query field, always use the internal field name `ComponentQuery` in the payload, not the UI label "Component GraphQL Query".

**API response note:** The `updatedFields` property in the API response may not list every field that was actually updated — it can show pre-existing field values. If unsure whether an update applied, verify in the Content Editor. The update still applies when the call returns a success status.

---

### Props Shape: With vs Without Component GraphQL Query

The presence or absence of a `Component GraphQL Query` on the rendering item fundamentally changes the shape of the props your React component receives. Match your React component's props interface to the chosen option — use either the GraphQL/jsonValue shape or the default PascalCase/.value shape, never a mix.

#### Summary Table

| Rendering has Component GraphQL Query   | Props shape                                                                                                      | Use in component                                         |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| **Yes** (list / parent–child)           | `fields.data.datasource` (parent fields + `datasource.children.results` for child items), camelCase, `jsonValue` | GraphQL-style interfaces with `jsonValue`                |
| **No** (simple / single-datasource)     | `fields` with PascalCase keys, `.value` per field                                                                | `fields.Title`, `fields.Description`, etc. with `.value` |
| **No datasource at all** (context-only) | No `fields` from datasource; read from `page.layout.sitecore.route.fields`                                       | `page` prop for route fields and context path            |

#### With Component GraphQL Query (list / parent–child components)

When the rendering has a `Component GraphQL Query` set, the layout service executes that query and returns data in a structured `data.datasource` shape. Children are nested **inside** the datasource object (not as a sibling).

- Datasource fields are under `fields.data.datasource`
- Field names use **camelCase** (as written in the GraphQL query)
- Each field has a `jsonValue` wrapper (e.g., `fields.data.datasource.title.jsonValue`)
- Children are at `fields.data.datasource.children.results` — an array of items each with `id` and field objects with `jsonValue`

**Example interface — ArticleCards:**

```typescript
interface ArticleCardChild {
  id: string;
  title?: { jsonValue?: Field<string> };
  image?: { jsonValue?: ImageField };
  link?: { jsonValue?: LinkField };
  badgeText?: { jsonValue?: Field<string> };
}

interface ArticleCardsFields {
  data?: {
    datasource?: {
      title?: { jsonValue?: Field<string> };
      children?: {
        results?: ArticleCardChild[];
      };
    };
  };
}
```

**Usage in JSX:**

```typescript
const datasource = fields?.data?.datasource;
const title = datasource?.title?.jsonValue;
const articles = datasource?.children?.results || [];
// <Text field={title} />
// articles.map(a => <Text field={a.title?.jsonValue} />)
```

**Example interface — TrustSection (children inside datasource):**

```typescript
interface TrustPointChild {
  id: string;
  icon?: { jsonValue?: ImageField };
  heading?: { jsonValue?: Field<string> };
  description?: { jsonValue?: Field<string> };
}

interface TrustSectionFields {
  data?: {
    datasource?: {
      title?: { jsonValue?: Field<string> };
      subtitle?: { jsonValue?: Field<string> };
      children?: {
        results?: TrustPointChild[];
      };
    };
  };
}
```

**Usage in JSX:**

```typescript
const datasource = fields?.data?.datasource;
const points = datasource?.children?.results || [];
// points.map(p => <Text field={p.heading?.jsonValue} />)
```

#### Without Component GraphQL Query (simple / single-datasource components)

When the rendering has NO `Component GraphQL Query` (field is empty), the layout service uses default JSS shaping:

- Fields are on `fields` directly at the top level — there is no `data.datasource` wrapper
- Field names use **PascalCase**, matching the Sitecore template field names exactly (e.g., `Title`, `Description`, `BackgroundImage`, `CtaLink`)
- Each field has a `value` property only — there is no `jsonValue`

**Example interface:**

```typescript
interface HeroFields {
  Title?: { value?: string };
  Description?: { value?: string };
  BackgroundImage?: { value?: { src?: string; alt?: string } };
  CtaLink?: { value?: { href?: string; text?: string } };
}
```

**Usage in JSX:**

```typescript
// <Text field={fields.Title} />
// <Image field={fields.BackgroundImage} />
// <Link field={fields.CtaLink} />
```

**Example default layout payload (no GraphQL query):**

```json
{
  "fields": {
    "Title": { "value": "GET YOUR MONEY MOVING WITH SAGA" },
    "Description": { "value": "Designed for people over 50..." },
    "BackgroundImage": { "value": {} },
    "CtaLink": { "value": { "href": "", "text": "Boost your savings" } }
  },
  "params": { "styles": "" },
  "rendering": { "componentName": "hero", "dataSource": "..." },
  "page": { "...": "..." }
}
```

---

### Datasource Folder Organization

To use query-based datasource locations, you need folder templates and folder instances.

**1. Create Folder Template:**

- Location: `/sitecore/templates/Project/<site-collection>/Folders/`
- Base template: Standard Template or Folder Template
- Name: `<Component> Folder` (e.g., "Hero Folder", "Article Cards Folder")
- Set Insert Options to allow your component templates as children
- **For list components (parent + child datasource):** Also add `_HorizonDatasourceGrouping` as a base template to the **parent component template** (not the folder template) so child items appear in the Pages Content tab. See [Children in XM Cloud Pages Content Tab](#children-in-xm-cloud-pages-content-tab) below for details.

**2. Create Folder Instance:**

- Location: `/sitecore/content/<site-collection>/<site-name>/Data/`
- Note: `<site-collection>` and `<site-name>` can be the same value (e.g., `saga-group/saga-group`). The first segment is the site collection (tenant), the second is the site within it.
- Use your folder template
- Name: Plural or descriptive (e.g., "Heroes", "ArticleCards")

**3. Create Datasource Items:**

- Location: Inside the folder instance
- Use your component template
- These become selectable in the datasource picker

**Example Structure:**

```
/sitecore/content/mysite/mysite/Data/
  └── ArticleCards (using "Article Cards Folder" template)
      ├── Article Cards Default (using "ArticleCards" template)
      │   ├── Julia Bradbury Packing (using "ArticleCard" child template)
      │   ├── Perfect Pancakes (using "ArticleCard" child template)
      │   └── Scammers Article (using "ArticleCard" child template)
      └── Homepage Articles (using "ArticleCards" template)
```

**Why This Pattern:**

- Better organization of datasources by type
- Reusable datasources across multiple pages
- Clearer content structure for authors
- Enables datasource picker filtering via query

---

### Children in XM Cloud Pages Content Tab

For components with parent/child datasources (e.g., ArticleCards, CategoryDirectory, MultiPromo), you can make child items appear in the **Content tab** of XM Cloud Pages so authors can add, sort, rename, and delete children without leaving the editor.

**Requirements:**

1. **Add `_HorizonDatasourceGrouping` as a base template** to the **parent datasource template** (e.g., `ArticleCards`, `CategoryDirectory`), not the child template and not the folder template.
   - Path: `/sitecore/templates/Foundation/Experience Accelerator/Horizon/_HorizonDatasourceGrouping`
   - Template ID: `{D0F6BE14-2A2D-4C56-ACB5-80CAA573B8E2}`
   - Ensure `Standard Template` is also a base template if not already inherited.

2. **Set Insert Options** on the parent template's **Standard Values** to allow the child template (e.g., `ArticleCard`, `CategoryDirectoryItem`). This controls which item types authors can add from the Content tab.

3. **Optional:** Add both parent and child templates to **FEaaS component data source templates** in site settings at `/sitecore/content/<site-collection>/<site-name>/Settings` if you want them available in the FEaaS component picker.

**Result:** When a component with this parent template is selected on a page, the Content tab shows a "Manage Items" section listing all child items with controls to sort (drag), rename, delete, and add new children — all inline without navigating away from the page.

**How to apply via MCP:**

```
1. Get the parent template item:
   get_content_item_by_path(path="/sitecore/templates/Project/mysite/Components/Cards/ArticleCards")

2. Update its __Base template field to include _HorizonDatasourceGrouping:
   update_content(
     itemId="<parent-template-id>",
     siteName="mysite",
     fields={
       "__Base template": "<existing-base-template-ids>|{D0F6BE14-2A2D-4C56-ACB5-80CAA573B8E2}"
     }
   )

3. Navigate to the parent template's Standard Values and set Insert Options
   to include the child template (e.g., ArticleCard template ID).
```

**When to use:**

- Any list component where the parent datasource has child items (cards, lists, FAQs, tabs, accordions)
- Components where content authors need to manage the number and order of child items

**When NOT to use:**

- Simple single-datasource components with no children (e.g., Hero, Banner)
- Components that source children from a query or external data rather than Sitecore child items

**Note:** The `_HorizonDatasourceGrouping` template is part of SXA/Horizon and is available in XM Cloud environments with SXA installed. If it does not exist at the path above, your environment may not have SXA — verify with `get_content_item_by_path` before attempting to add it as a base template.

---

### SEO Metadata Template Extension

Sitecore XM Cloud / SXA includes a built-in `_Seo Metadata` template that provides base SEO fields.

**Do not modify** the out-of-the-box `_Seo Metadata` template directly. Instead, create a custom template that inherits from it and adds project-specific SEO fields.

- **OOB template:** `_Seo Metadata` (ID: `{9B5B9E0E-1CEF-4D54-8C47-27F241CE075B}`)
- **Location for custom extension:** `/sitecore/templates/Project/<site-collection>/Presentation/_Seo Metadata Extended`

**Creation workflow:**

1. Create a new template (e.g., `_Seo Metadata Extended`) under the `Presentation/` template folder.
2. Set its `__Base template` to include `_Seo Metadata` (`{9B5B9E0E-1CEF-4D54-8C47-27F241CE075B}`).
3. Add a `Data` section with any additional fields your project needs (e.g., MetaTitle, CanonicalUrl, OpenGraphTitle, OpenGraphDescription, OpenGraphImage, TwitterTitle, TwitterDescription, TwitterImage, Robots).
4. Add the new template as a base template to your Page template(s) so all pages inherit the SEO fields.

**Key rules:**

- Never edit OOB SXA/Foundation templates — always extend via inheritance.
- Keep custom SEO templates under `Presentation/` alongside other page-level templates.
- Page templates that need SEO fields should inherit from your custom extension, not from `_Seo Metadata` directly (so you have a single place to add new fields later).

---

### Component Registration Workflow

After creating all Sitecore items, the React component must be registered in the Next.js application.

**1. Component File:**

- Path: `src/components/<category>/<component-name>.tsx`
- Use kebab-case for filename: `article-cards.tsx`, `hero.tsx`
- Must export named variants: `export const Default`, `export const ImageBottom`, etc.
- Use `ComponentProps` from `@/lib/component-props`
- Import ONLY from `@sitecore-content-sdk/nextjs` (never from `/config-cli` or `/tools`)

**2. Register in Component Map:**

- File: `.sitecore/component-map.ts`
- Import: `import * as articlecards from 'src/components/ui/article-cards';`
- Map entry: `['article-cards', { ...articlecards, componentType: 'client' }]`
- The key MUST match the rendering's `componentName` field exactly

**Example Registration:**

```typescript
// .sitecore/component-map.ts
import * as articlecards from "src/components/ui/article-cards";
import * as hero from "src/components/ui/hero";

export const componentMap = new Map<string, NextjsContentSdkComponent>([
  ["article-cards", { ...articlecards, componentType: "client" }],
  ["hero", { ...hero }],
]);
```

**3. Component Code Pattern:**

Choose the pattern that matches your rendering configuration. If the rendering has a `Component GraphQL Query`, use the GraphQL/jsonValue pattern. If the field is empty, use the default JSS pattern.

**Pattern A: With Component GraphQL Query (list / parent–child, e.g., ArticleCards)**

```typescript
'use client';

import type React from 'react';
import { Text, Image, Link } from '@sitecore-content-sdk/nextjs';
import { Field, ImageField, LinkField } from '@sitecore-content-sdk/nextjs';
import { ComponentProps } from '@/lib/component-props';

// GraphQL shape: fields.data.datasource (with children inside), camelCase, jsonValue
interface ArticleCardChild {
  id: string;
  title?: { jsonValue?: Field<string> };
  image?: { jsonValue?: ImageField };
  link?: { jsonValue?: LinkField };
  badgeText?: { jsonValue?: Field<string> };
}

interface ArticleCardsFields {
  data?: {
    datasource?: {
      title?: { jsonValue?: Field<string> };
      children?: {
        results?: ArticleCardChild[];
      };
    };
  };
}

interface ArticleCardsProps extends ComponentProps {
  params: { [key: string]: any };
  fields: ArticleCardsFields;
}

export const Default: React.FC<ArticleCardsProps> = (props) => {
  const { fields, page } = props;
  const isEditing = page.mode.isEditing;
  const datasource = fields?.data?.datasource;
  const title = datasource?.title?.jsonValue;
  const articles = datasource?.children?.results || [];

  return (
    <section>
      {title && <Text field={title} />}
      {articles.map((article) => (
        <div key={article.id}>
          <Image field={article.image?.jsonValue} />
          <Text field={article.title?.jsonValue} />
          <Link field={article.link?.jsonValue} />
        </div>
      ))}
    </section>
  );
};
```

**Pattern B: Without Component GraphQL Query (simple / single-datasource, e.g., Hero)**

```typescript
'use client';

import type React from 'react';
import { Text, Image, Link } from '@sitecore-content-sdk/nextjs';
import { ComponentProps } from '@/lib/component-props';

// Default JSS shape: fields at top level, PascalCase, .value
interface HeroFields {
  Title?: { value?: string };
  Description?: { value?: string };
  BackgroundImage?: { value?: { src?: string; alt?: string } };
  CtaLink?: { value?: { href?: string; text?: string } };
}

interface HeroProps extends ComponentProps {
  params: { [key: string]: any };
  fields: HeroFields;
}

export const Default: React.FC<HeroProps> = (props) => {
  const { fields, page } = props;
  const isEditing = page.mode.isEditing;

  return (
    <section>
      <Text field={fields.Title} />
      <Text field={fields.Description} />
      <Image field={fields.BackgroundImage} />
      <Link field={fields.CtaLink} />
    </section>
  );
};
```

**4. Verification Checklist:**

- Component appears in Pages editor component toolbar
- Datasource picker shows available items when adding to page
- Fields are editable inline in editing mode
- Component renders correctly on the published site
- For list components: children appear in the Content tab with sort/add/delete controls (requires `_HorizonDatasourceGrouping` on parent template)

---

## Content Tree — Core Paths

### Pages (created via `create_page`)

Pages are content items that live under the site's Home node.

- **Path:** `/sitecore/content/<site-collection>/<site-name>/Home/...`
- Pages must be created using a valid page template.
- Use `get_all_pages_by_site` or `search_site` to discover existing pages before creating new ones.
- Use `get_page_template_by_id` to verify template fields and settings before page creation.

### Content Items (created via `create_content_item`)

Generic content items can be created at any valid location where the template's insert options allow.

- **Site Data folder (shared datasources):** `/sitecore/content/<site-collection>/<site-name>/Data/<component-type>/`
- **Route-level datasources (page-local):** `/sitecore/content/<site-collection>/<site-name>/Home/<page-name>/Data/`
- **Global shared content:** `/sitecore/content/<site-collection>/<site-name>/Data/`

Always use `list_available_insertoptions` to confirm which templates are valid children at a given parent path before creating a content item.

### Component Datasources (created via `create_component_datasource`)

- **Page-level (local) datasources:** Under the page item itself, typically in a `Data` or `Page Components` child folder.
- **Site-level (shared) datasources:** `/sitecore/content/<site-collection>/<site-name>/Data/<component-folder>/`

Use `search_component_datasources` to find existing datasources that can be reused rather than creating duplicates.

### Assets (uploaded via `upload_asset`)

Digital assets are stored in the Media Library:

- **Site-specific media:** `/sitecore/media library/Project/<site-collection>/<site-name>/`
- **Shared media:** `/sitecore/media library/Project/<site-collection>/shared/`

Use `search_assets` to find existing assets before uploading new ones to avoid duplication.

---

## Definition Items (Developer-Owned)

These items define the structure and configuration of the implementation and are typically serialized:

- **Templates:** `/sitecore/templates/Project/<site-collection>/`
- **Branch templates:** `/sitecore/templates/Branches/Project/<site-collection>/`
- **Layouts:** `/sitecore/layout/Layouts/Project/<site-collection>/`
- **Renderings:** `/sitecore/layout/Renderings/Project/<site-collection>/`
- **Placeholder settings:** `/sitecore/layout/Placeholder Settings/Project/<site-collection>/`
- **Modules:** `/sitecore/system/settings/Project/<site-collection>/`

---

## SXA Site Structure

For sites built with Sitecore Experience Accelerator (SXA):

- **Home (pages):** `/sitecore/content/<site-collection>/<site-name>/Home/`
- **Data (shared datasources):** `/sitecore/content/<site-collection>/<site-name>/Data/`
- **Media (site media references):** `/sitecore/content/<site-collection>/<site-name>/Media/`
- **Dictionary:** `/sitecore/content/<site-collection>/<site-name>/Dictionary/`
- **Presentation:** `/sitecore/content/<site-collection>/<site-name>/Presentation/`
- **Settings:** `/sitecore/content/<site-collection>/<site-name>/Settings/`

---

## Common Template IDs Reference

| Template Name               | Template ID                            | Purpose                                             |
| --------------------------- | -------------------------------------- | --------------------------------------------------- |
| Template                    | `ab86861a-6030-46c5-b394-e8f99e8b87db` | Base for custom templates                           |
| Template Section            | `e269fbb5-3750-427a-9149-7aa950b49301` | Groups fields within template                       |
| Template Field              | `455a3e98-a627-4b40-8035-e683a0331ac7` | Individual field definition                         |
| JSON Rendering              | `04646a89-996f-4ee7-878a-ffdbf1f0ef0d` | Headless component rendering                        |
| Folder                      | `a87a00b1-e6db-45ab-8b54-636fec3b5523` | Organizing items                                    |
| JSS Data                    | `a29d272e-9d48-453c-9e9d-b47585fa7f20` | Data folder for SXA sites                           |
| \_HorizonDatasourceGrouping | `D0F6BE14-2A2D-4C56-ACB5-80CAA573B8E2` | Enables child management in Pages Content tab (SXA) |
| \_Seo Metadata              | `9B5B9E0E-1CEF-4D54-8C47-27F241CE075B` | SXA SEO base template (do not modify — extend it)   |

---

## Complete Example: Creating an ArticleCards Component

### Step 1: Create Templates via MCP

**Parent template (ArticleCards):**

```
create_content_item(
  name="ArticleCards",
  templateId="ab86861a-6030-46c5-b394-e8f99e8b87db",
  parentId="<project-templates-folder-id>"
)
# Create "Data" section, then "Title" field (Single-Line Text)
# Add _HorizonDatasourceGrouping as base template (see Step 1b)
```

**Child template (ArticleCard):**

```
create_content_item(
  name="ArticleCard",
  templateId="ab86861a-6030-46c5-b394-e8f99e8b87db",
  parentId="<project-templates-folder-id>"
)
# Create "Data" section, then fields: Image, Title, Link, BadgeText
```

**Step 1b: Enable child management in Pages Content tab:**

```
# Add _HorizonDatasourceGrouping as a base template to the PARENT template (ArticleCards)
update_content(
  itemId="<articlecards-template-id>",
  siteName="mysite",
  fields={
    "__Base template": "<existing-base-template-ids>|{D0F6BE14-2A2D-4C56-ACB5-80CAA573B8E2}"
  }
)

# Set Insert Options on ArticleCards Standard Values to allow ArticleCard as a child
# This enables the "Add" button in the Pages Content tab
```

### Step 2: Create Folder Template

```
create_content_item(
  name="Article Cards Folder",
  templateId="ab86861a-6030-46c5-b394-e8f99e8b87db",
  parentId="<project-templates-folder-id>"
)
# Set insert options to allow ArticleCards template
```

### Step 3: Create Rendering

```
# Renderings mirror the template folder structure
create_content_item(
  name="ArticleCards",
  templateId="04646a89-996f-4ee7-878a-ffdbf1f0ef0d",
  parentId="<cards-renderings-folder-id>"
)

update_content(
  itemId="<rendering-id>",
  siteName="mysite",
  fields={
    "componentName": "article-cards",
    "Datasource Template": "/sitecore/templates/Project/mysite/Components/Cards/ArticleCards",
    "Datasource Location": "query:$site/*[@@name='Data']/*[@@templatename='Article Cards Folder']|query:$sharedSites/*[@@name='Data']/*[@@templatename='Article Cards Folder']",
    "AddFieldEditorButton": "1",
    "ComponentQuery": "query ArticleCards($datasource: String!, $language: String!) { datasource: item(path: $datasource, language: $language) { ... on ArticleCards { title { jsonValue } } children { results { ... on ArticleCard { id title { jsonValue } image { jsonValue } link { jsonValue } badgeText { jsonValue } } } } } }"
  }
)
```

### Step 4: Create Datasource Structure

```
# Create folder instance in Data
create_content_item(
  name="ArticleCards",
  templateId="<article-cards-folder-template-id>",
  parentId="<site-data-folder-id>"
)

# Create parent datasource
create_content_item(
  name="Article Cards Default",
  templateId="<articlecards-template-id>",
  parentId="<articlecards-folder-id>",
  fields={"Title": "Latest Articles"}
)

# Create child items
create_content_item(
  name="Julia Bradbury Article",
  templateId="<articlecard-template-id>",
  parentId="<parent-datasource-id>",
  fields={"Title": "Julia Bradbury: My worst packing disaster", "BadgeText": "ARTICLE"}
)
```

### Step 5: Create React Component

File: `src/components/ui/article-cards.tsx`

### Step 6: Register in Component Map

File: `.sitecore/component-map.ts`

```typescript
import * as articlecards from "src/components/ui/article-cards";

export const componentMap = new Map([
  ["article-cards", { ...articlecards, componentType: "client" }],
]);
```

### Step 7: Add to Page

1. Open the page in Sitecore Pages editor
2. Click "Add Component" on a placeholder
3. Find "ArticleCards" in the component list
4. Select datasource from the picker
5. Component appears on page with editable fields
6. In the Content tab, child items (ArticleCard) appear with sort, add, rename, and delete controls (enabled by `_HorizonDatasourceGrouping`)

---

## Common Issues and Solutions

### Template Creation

**`list_available_insertoptions` fails on template folders:**
This is expected — the tool requires a site root ancestor. Not applicable to template folders.

**Fields not appearing in datasource picker:**
Ensure the template has the proper 3-level structure: Template → Section → Fields.

### Rendering Configuration

**"No datasource template found for component" when adding to page:**

- Verify `Datasource Template` contains the full path (not a GUID)
- Path must start with `/sitecore/templates/...`
- Confirm the template actually exists at that path

**Component not appearing in Pages editor toolbar:**

- Verify `componentName` is set and matches the component-map key (kebab-case)
- Confirm component is registered in `.sitecore/component-map.ts`
- Rendering template must be "JSON Rendering" (not "Controller Rendering")

**Datasource picker is empty:**

- Verify the `Datasource Location` query is correct
- Folder template name in query must match the actual folder template exactly
- Folder instance must exist in the Data folder
- Folder must contain datasource items as children

**Context-only component shows empty content:**

- Verify the component reads from `page.layout.sitecore.route.fields`, not from `fields` (context-only components have no datasource fields)
- Ensure the page template includes the expected route fields (e.g., Title, Content, MainImage)
- Confirm the rendering has `Datasource Template` and `Datasource Location` both empty — if either is set, Sitecore expects a datasource

**Component renders parent data but children are missing:**

- The `ComponentQuery` field on the rendering is either empty or missing the `children { results { ... } }` block
- Verify the template names in `... on <TemplateName>` fragments match the actual Sitecore template names (PascalCase)
- Verify all needed fields are listed inside the fragment with `{ jsonValue }`
- Check that field names in the query use camelCase (e.g., `badgeText` not `BadgeText`)
- For list components, you must set the query and use `fields.data.datasource.children.results` in the component

**Component expects `fields.data.children.results` but array is empty:**

- Children are nested **inside** the datasource object, not as a sibling. Use `fields.data.datasource.children.results`, not `fields.data.children.results`.

**Component expects `fields.data.datasource` but receives `fields.Title` (or vice versa):**

- This is a props shape mismatch between the rendering configuration and the component code
- If the rendering has NO `Component GraphQL Query`: the component receives the default JSS shape — fields at top level with PascalCase keys and `.value` (e.g., `fields.Title.value`, `fields.Description.value`). Use the default pattern in the component.
- If the rendering HAS a `Component GraphQL Query`: the component receives the GraphQL shape — `fields.data.datasource` with camelCase keys and `.jsonValue`. Use the GraphQL pattern in the component.
- Fix: either add a GraphQL query to the rendering and update the component to the `data.datasource`/`jsonValue` shape, or remove the query and update the component to the PascalCase/`.value` shape. Never mix both.

**Children not appearing in Pages Content tab:**

- Add `_HorizonDatasourceGrouping` (`{D0F6BE14-2A2D-4C56-ACB5-80CAA573B8E2}`) as a base template to the **parent** datasource template (e.g., ArticleCards, CategoryDirectory) — not the child template and not the folder template.
- Set Insert Options on the parent template's **Standard Values** to allow the child template. Without Insert Options, the "Add" button in the Content tab won't know which templates to offer.
- Verify the `_HorizonDatasourceGrouping` template exists at `/sitecore/templates/Foundation/Experience Accelerator/Horizon/_HorizonDatasourceGrouping`. If it doesn't exist, your XM Cloud environment may not have SXA installed or the path may differ.
- Ensure child items actually exist under the parent datasource item in the content tree. The Content tab reflects the real item hierarchy.

### Component Registration

**Component renders but fields are not editable:**

- Use Sitecore field components (`Text`, `Image`, `Link`)
- Access fields via the `jsonValue` property
- Handle `isPageEditing` prop correctly

**Component name mismatch errors:**
Verify the entire naming chain matches:

- File: `article-cards.tsx`
- Import: `import * as articlecards`
- Map key: `['article-cards', {...}]`
- Rendering: `componentName: "article-cards"`

**TypeScript errors in component:**

- Import only from `@sitecore-content-sdk/nextjs` (main package)
- Never import from `/config-cli`, `/tools`, or `/client` subpaths
- Props interface must extend `ComponentProps`
- Use optional chaining (`?.`) for all nested field access

### Build and Deployment

**Component not appearing after registration:**

- Restart the Next.js dev server after changes to `component-map.ts`
- Clear the `.next` cache folder and rebuild if needed

---

## Key Rules When Creating Items via MCP

1. **Always verify the parent path** — Use `get_content_item_by_path` before creating items to confirm the path exists and the template is allowed.
2. **Respect insert options** — Use `list_available_insertoptions` to check which templates can be created under a given parent. Never force-create items where their template is not permitted.
3. **Use the correct database** — Content items are created in the Master database. Items must be published to become live.
4. **Template structure matters** — Templates need the 3-level hierarchy: Template → Template Section → Template Fields. Missing the section causes rendering issues.
5. **Always use JSON Rendering** — Use template ID `04646a89-996f-4ee7-878a-ffdbf1f0ef0d` for headless components. Never use "Controller Rendering" or "View Rendering."
6. **Component names are kebab-case** — The `componentName` field in rendering, the component-map key, and the filename must all use kebab-case and match exactly.
7. **Datasource Template uses full paths** — The `Datasource Template` field requires the complete path starting with `/sitecore/templates/...`, never a GUID.
8. **Datasource Location uses query syntax** — Queries must use exact folder template names with `@@templatename`. Separate multiple locations with `|`.
9. **Create folder templates** — Use folder templates and folder instances for better datasource organization and query-based selection.
10. **Avoid duplicates** — Use search tools (`search_site`, `search_assets`, `search_component_datasources`) before creating new items.
11. **Assets go in Media Library** — Never create media items under `/sitecore/content/`. Always use `/sitecore/media library/Project/...` paths.
12. **Restart after registration** — Component map changes require a Next.js dev server restart to take effect.
13. **Use optional chaining** — Always use `?.` when accessing nested field properties in React components.
14. **Child datasources for lists** — Components that render lists need datasource items with children, not multilist fields. Children are nested inside the datasource object: access them via `fields.data.datasource.children.results`. The rendering's `ComponentQuery` field MUST include a query with `children { results { ... on <ChildTemplate> { } } }` or child data will not be passed to the component.
15. **Simple vs list component props** — For components without children, leave `ComponentQuery` empty and consume the default props (`fields` with PascalCase keys and `.value`). For components with children, set the GraphQL query and consume `fields.data.datasource` (parent fields + `datasource.children.results` for child items) with `jsonValue` and camelCase. Never mix the two shapes in a single component.
16. **Manage Items in Pages** — For list components (parent + child datasource), add `_HorizonDatasourceGrouping` (`{D0F6BE14-2A2D-4C56-ACB5-80CAA573B8E2}`) as a base template to the **parent** template so child items appear in the Pages Content tab with sort, add, rename, and delete controls. Set Insert Options on the parent's Standard Values to allow the child template. This is only for list components — do not add it to simple single-datasource templates.
17. **Context-only components need no datasource** — For components that display the current page's own fields (breadcrumbs, article headers, body content), leave `Datasource Template`, `Datasource Location`, and `ComponentQuery` all empty on the rendering. The component reads from `page.layout.sitecore.route.fields` instead of `fields`. Ensure the page template includes the fields the component expects.
18. **Never modify OOB templates** — Do not edit Sitecore's built-in Foundation or SXA templates (e.g., `_Seo Metadata`, `_HorizonDatasourceGrouping`). Always extend them by creating a custom template that inherits from the OOB template and adding your fields there.

---

## Workflow Considerations

- **Publishing after MCP changes:** All items created or updated via MCP live in the Master database only. They are NOT visible on the live site or Experience Edge until published. After creating or updating content via MCP, always publish (Smart Publish for targeted items, Incremental Publish for bulk updates) so changes take effect.
- Pages and content items may be subject to publishing workflows. Check if a workflow is assigned to the template's standard values before expecting items to be live.
- Marketing definitions follow the Analytics workflow: Draft → Deployed. Use the Deploy action to activate.
- Component registration changes (`component-map.ts`) require application restart/rebuild to take effect — this is a code change, not a publishing action.

---

## Reference Documentation

- Marketer MCP tools reference: https://doc.sitecore.com/sai/en/users/sitecoreai/marketer-mcp-tools-reference.html
- Marketer MCP overview: https://doc.sitecore.com/sai/en/users/sitecoreai/marketer-mcp.html
- Agent API: https://api-docs.sitecore.com/ai-capabilities/agent-api
- Content SDK Documentation: https://doc.sitecore.com/xmc/en/developers/content-sdk/
- Sitecore Query Syntax: https://doc.sitecore.com/xp/en/developers/latest/platform-administration-and-architecture/sitecore-query.html
